<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bulletin">

	<resultMap id="bulletinMap" type="com.larkin.web.model.bulletin.BulletinModel">
		<result column="bulletin_id" property="bulletinId"/>
		<result column="title" property="title"/>
		<result column="content" property="content"/>
		<result column="type" property="type"/>
		<result column="status" property="status"/>
		<result column="create_time" property="createTime"/>
	</resultMap>

	<select id="getHelpInfo" parameterType="map" resultMap="bulletinMap">
		select * from bulletin where type=#{type} order by create_time desc limit 1;
	</select>

	<select id="getBulletins" parameterType="map" resultMap="bulletinMap">
		select * from bulletin where type!=#{type} order by create_time desc limit #{rows},#{offset};
	</select>

	<insert id="doBulletin" parameterType="com.larkin.web.model.bulletin.BulletinModel">
		INSERT INTO bulletin(title, content, type, status, create_time)
		VALUES (#{title}, #{content}, #{type}, #{status}, #{createTime});
	</insert>



</mapper>