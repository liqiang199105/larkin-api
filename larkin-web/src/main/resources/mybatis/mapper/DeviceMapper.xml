<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="device">
	<resultMap id="deviceMap" type="com.larkin.web.model.device.DeviceModel">
		<result column="device_id" property="deviceId"/>
		<result column="ip" property="ip"/>
		<result column="os" property="os"/>
		<result column="last_modified" property="lastModified"/>
	</resultMap>

	<insert id="recordDevice" useGeneratedKeys="true" keyProperty="collectionId" parameterType="com.larkin.web.model.device.DeviceModel">
		replace into device(device_id,ip,os,last_modified)
        values(#{deviceId},#{ip},#{os},#{lastModified});
	</insert>

    <select id="getDeviceInfo" parameterType="String" resultMap="deviceMap">
        SELECT * from device where device_id = #{deviceId}
    </select>

</mapper>